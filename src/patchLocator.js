"use strict";


function patchLocator (acceptPatchHeader, patchMediaTypes) {
    // find the header
    // build a patchDiff object for that accept type
    // otherwise fail
}

module.exports = patchLocator;